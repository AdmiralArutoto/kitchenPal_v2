# CLAUDE.md — KitchenPal Living Memory

**Source-of-truth specs:** base architecture [.claude/plans/SESSION_2_SPEC.md](.claude/plans/SESSION_2_SPEC.md) · roadmap [.claude/plans/SESSION_3_IMPLEMENTATION.md](.claude/plans/SESSION_3_IMPLEMENTATION.md) · import feature [.claude/plans/IMPORT_FEATURE_SPEC.md](.claude/plans/IMPORT_FEATURE_SPEC.md) · setup the agent can't do [STARTUP.md](STARTUP.md).
**Decision log (the dated "why"):** [.claude/DECISIONS.md](.claude/DECISIONS.md).

This file is the **current state** of the work + the live **architecture** (kept accurate by editing in place), plus error patterns and gotchas. It is NOT a changelog — dated history lives in the decision log.

---

## Maintaining this memory (read first)

**After any notable change or decision, before ending the turn:**
1. **Append a dated entry to [.claude/DECISIONS.md](.claude/DECISIONS.md)** — template `**[YYYY-MM-DD] — Short title**` then the *why*: what was chosen, the alternative rejected, and how to apply / what not to undo. Append at the bottom; never rewrite past entries.
2. **If the change alters the architecture, update the matching `## Architecture` bullet below IN PLACE** to reflect reality — do NOT append a dated block to this file.
3. **Refresh `## Current State`** (status / last session / next action) each session.
4. Record new gotchas under `Error Patterns` / `Undocumented Dependencies` / `Spec Conflicts`.

"Notable" = anything a future agent could undo by accident, any non-obvious trade-off, any new dependency/route/pattern. Skip trivial/self-evident changes. (These are model-followed conventions, not harness-enforced.)

---

## Current State

```
Status: Recipe Import — Phase 1 shipped (websites + manual paste). PRODUCT SHIFT made concrete in the v2 redesign (Session 32): a **global Nav "+ Add Recipe"** (state in `AuthedLayout` via `AddRecipeContext`) opens an **import-first chooser with the URL field INLINE**; Import → `ImportModal` opens on a **platform-confirm** step (client-side `detectPlatform`, display-only) → Extract. Manual → `AddRecipeModal`, Generate → `GenerateModal` (the AI gen flow moved OFF Home into a modal). **Home is now recommendations-first**: "Ideas for tonight" (`DailyRotationFeed`, top, + a live "Refreshes in Xh" UTC-midnight countdown + FRESH TODAY badge) over a `CatalogPreview` row (recent recipes + "View all →" to /catalog). Nav links Home/Catalog/Cook Mode. Import flow: paste URL → POST /api/import (Node-only) classifies the host, fetches HTML, parses schema.org JSON-LD (with @graph walk) → maps to KitchenPal draft; ingredient strings parsed regex-first → gpt-4o-mini fallback. No JSON-LD → strips chrome and sends cleaned text to gpt-4o-mini extraction. VIDEO URLs: YouTube → extractFromVideo, now DESCRIPTION-FIRST (Supadata /youtube/video metadata ~3s → extraction LLM; the recipe is usually in the description like TikTok), falling back to the Supadata transcript only when transcriptLanguages is non-empty (so caption-less Shorts fast-fail to the paste fallback instead of a ~25s transcript-generation wait). Instagram/TikTok → Apify (scrapes caption + comments — where the recipe usually is) run ASYNC: /api/import starts the Apify run + returns {runId,datasetId} (202), the client polls /api/import/poll (no DB; stateless), and on SUCCEEDED the server runs a LAZY CASCADE (link in caption/creator-comment → extractFromWebsite | else caption+top-5-comments → LLM | else Supadata transcript merge), stopping at the first complete recipe (≥2 ingredients + ≥1 step). Comments are authoritative over transcript (IMPORT_SOCIAL_SYSTEM_PROMPT). Failures (no transcript, blocked, async >~40s budget) surface as 422 → UI auto-switches to the manual fallback. The fallback view (ImportModal `paste` phase) is a TabToggle: "Paste text" (POST /api/import/text, works for every platform) and "Screenshot" (POST /api/import/image — multipart upload → gpt-4o-mini VISION extraction with an optional user note, image never stored). Draft is reviewed in the shared RecipeEditForm and saved via the existing POST /api/recipes with source:'imported' + sourceUrl/sourcePlatform/sourceCreator. Attribution strip ("From {creator} · {host}") shows in the import draft and in RecipeModal view mode for imported recipes. Progress UX shows REAL server-driven stages: website/YouTube STREAM stages over SSE (POST /api/import → text/event-stream: fetching → reading-structured|ai-extracting → parsing-ingredients; fetching-transcript → transcribing → extracting), and IG/TikTok report real coarse stages via the poll (queued/scraping → extracting → done, cascade runs on a finalize:true poll). lib/sse.ts + an onStage callback threaded through the extractors; ImportModal renders the live stages (see .claude/stages/stage-import-progress.md). MODIFY: existing-recipe "Modify with AI" now opens a side-by-side `ModifyStudio` (quick controls compose one NL comment, auto-applied on a 600ms debounce against the ORIGINAL recipe → Save as copy / Replace original / Undo all); `POST /api/ai/modify` returns `{recipe, diff}` where the diff is computed deterministically server-side in `lib/diff.ts` (LCS align + token diff), NOT by the LLM. Build clean (api + web); 104/104 backend tests green.
STAGE 7 (Vercel deploy) DONE & WORKING: single Vercel project — Vite SPA static + the Express app as ONE serverless function (`api/index.ts`), Hobby, `maxDuration: 60`, prod DB = same Supabase project as dev (build runs `prisma generate` only). Four cold-start blockers fixed in order (see DECISIONS [2026-05-29]): Vercel Root Directory, ESM/CJS dynamic-import shim, Node 22 for Supabase WebSocket, Prisma engine includeFiles. WORKFLOW: develop on `dev` branch → pushes auto-build PROTECTED previews (Vercel Standard Protection, free); `main` is the promote-when-ready production branch (NOT pushed yet). Production protection is Pro-only on Hobby, so we stay preview-only (private) until launch; at launch, set the Supabase signup policy.
Last session: Session 33 — **"Culinary Serenity" visual reskin** (DECISIONS [2026-06-01]; Figma `PmyY8PrGtVZ0QvsiFigRGU` + `.tmp-import/DESIGN.md`). **Visual-only** (mechanics/layouts/wireframes untouched; the Figma's left-sidebar IA NOT adopted): rewrote `index.css @theme` VALUES (names stable) → parchment surfaces + **deep-sage primary** + **terracotta featured-accent** + warm-charcoal text + deep-sage footer; image-fallback blob retuned terracotta→ochre→sage via 2 tokens. Added **Source Serif 4** (`index.html` + `--font-serif`) on ~18 editorial headings (recipe/page/section titles; in-recipe label utility headers stay Inter). New **`accent` Button variant** (terracotta) on Import/Extract CTAs + chooser Recommended badge; RecipeModal step numerals → serif-terracotta. Softened `Card` shadow. **User-decided: sage-primary + terracotta-accent** (rejected terracotta-primary). Hex-guard grep clean (only `index.css`); web build clean; lint = 2 pre-existing `_next`/`_omit` only. NOT yet run in-browser / pushed.
Also Session 33 — **recipe detail modal** reworked twice (DECISIONS [2026-06-01]): first to Figma 47:1323 (asymmetric), then **adjusted to 47:1014** (current — see Architecture): two equal columns over a **sticky Modify/Edit/Delete footer**, ingredients table **back in the right parchment sidebar** (under a SCALE card), source/video link reused in the left column, image keeps the shared top-right **`ImageActionMenu`**. The create-flow `useImagePicker` shows **three centered buttons inside the image** that dismiss on choose (Generate → spinner cue, real gen still post-save). Build clean (187 modules), hex-guard + lint clean (2 pre-existing api errors only).
Next action: Smoke-test in `npm run dev` — the reskin (parchment surfaces, sage primary buttons + focus rings, terracotta Import/Extract CTAs, serif titles, deep-sage footer, image-fallback wash) PLUS the new RecipeModal (equal 2-col, sticky footer stays in view while scrolling, ingredients table in the right sidebar, image action menu Generate/Upload/Remove, create-flow centered picker + generate spinner). Then push `dev` to the Vercel preview. DEFERRED (carried from S32): SSE incremental-stream verification on Vercel; compact rec tiles; Modify studio as a full page + wiring into import-draft/manual-create; import Saved/Open/+Another state; SSE for paste/screenshot; real Settings controls; avatar orphan cleanup on remove.
Open questions: None active.
```

---

## Architecture

> How the system works **now**. Edit in place as it changes. Rationale/history → [.claude/DECISIONS.md](.claude/DECISIONS.md).

### Stack & layout
- **npm workspaces** monorepo: `apps/api` (Express 5, ESM — imports use `.js` extensions) + `apps/web` (Vite + React 18 + react-router-dom). Root `npm run dev` runs both via `concurrently` (tsx watch on :3001 + Vite, `/api/*` proxied to :3001).
- **Tailwind v4** via `@tailwindcss/vite`; theme tokens live in `apps/web/src/index.css` `@theme` (no `tailwind.config`, no hex literals in JSX). ESLint v9 flat config at repo root.
- **Design language = "Culinary Serenity"** (DECISIONS [2026-06-01]): warm **parchment** surfaces, **deep-sage primary** (`--color-primary`), **terracotta featured-accent** (`--color-accent` → `Button variant="accent"`, used on Import/Extract CTAs + step numerals), **Source Serif 4** (`--font-serif`) on editorial headings (recipe titles + page/section titles) over **Inter** body. The reskin was a pure `@theme` **value** remap — token **names are stable** (the whole-app cascade depends on it); don't re-introduce orange/blue-grey or rename tokens. Fonts loaded in `index.html`.
- **Env:** root `.env` is the single source; backend scripts wrapped with `dotenv -e ../../.env`; Vite reads it via `envDir: '../..'`. Core keys (`SUPABASE_*`, `OPENAI_API_KEY`) throw at module import; service keys (`SUPADATA_API_KEY`, `APIFY_TOKEN`) are read **at call time / fail-soft** — a missing one disables only that feature, not the whole function.

### Backend (`apps/api/src`)
- Routes throw `HttpError(status, msg)` → error middleware → `{ error }`. `authMiddleware` verifies the Supabase JWT, upserts the `Profile`, sets `req.userId` / `req.userEmail`; every route except `GET /api/health` is authed.
- **Tenancy is app-layer; Supabase RLS is OFF** — every query is scoped to `req.userId`. Don't enable RLS without updating every route + a DECISIONS entry.
- **Zod schemas in `schemas/` are the source of truth** — types via `z.infer`, never hand-declared interfaces.
- **OpenAI** via `lib/openai.ts` `callOpenAIJson` / `callOpenAIVisionJson` (share a private `completeJson`, JSON mode). Client `timeout: 30_000, maxRetries: 1`; import calls pass an explicit `timeoutMs` (→ single attempt, bounded). Model split: `gpt-4o-mini` (drafts, import extraction, vision) vs `gpt-4o` (generate-full, modify, daily-rotation normalize).

### Data model (Prisma → Supabase Postgres)
- `Recipe` (content fields + `source` ∈ `manual|ai_generated|ai_modified|daily_rotation|imported` + `source_url/platform/creator` + `image_url`), `Profile` (preferences), `DailyBatch` (per-user per-UTC-day cache). Migrations are applied **locally** (`prisma migrate dev`) against the shared cloud DB (dev and prod share one Supabase project).

### Recipe import — the headline feature → [IMPORT_FEATURE_SPEC.md](.claude/plans/IMPORT_FEATURE_SPEC.md)
- Global Nav **+ Add Recipe** (state lifted to `AuthedLayout` via `contexts/AddRecipeContext.tsx`, so any page opens one chooser instance) → import-first `AddRecipeChooser` (URL field **inline** in the dominant Import card; Manual → `AddRecipeModal`, Generate → `GenerateModal`). Import hands the URL to `ImportModal` (`initialUrl` prop), which opens on a **platform-confirm** step — a detected-source card from client-side `detectPlatform`/`normalizeUrl` (`lib/import.ts`; display-only, no metadata fetch) + Extract. Server `classifyUrl` re-classifies on Extract and routes:
  - **website** → JSON-LD (schema.org, `@graph` walk) else strip-chrome → LLM; **YouTube** → **description-first** (Supadata `/youtube/video` metadata ~3s; the recipe is usually in the description, like TikTok) → LLM, then **transcript fallback only when `transcriptLanguages` is non-empty** (skips a ~25s wait on caption-less Shorts). Channel name → creator attribution. Both **synchronous** and **stream real stages over SSE**.
  - **Instagram / TikTok** → Apify, **async start + client poll** (no DB; Apify holds the run), then a **lazy cascade** — link in caption/creator-comment → `extractFromWebsite` | caption + top-5 comments → LLM | Supadata transcript merge — stopping at the first complete recipe (≥2 ingredients + ≥1 step). IG recipe lives in the pinned comment; TikTok in the description.
  - **paste text** / **screenshot (vision)** = manual fallbacks (offered automatically on any failure).
- Drafts are **never persisted by import routes** — reviewed in `RecipeEditForm`, saved via `POST /api/recipes` (`source:'imported'` + `source_*`). `SourceAttribution` strip shows on imported recipes.

### AI generation & daily rotation
- Generation is **secondary**: the `generate-drafts → generate-full` flow lives in `components/GenerateModal.tsx` (launched from the chooser), **no longer on Home**. Home is recommendations-first — `DailyRotationFeed` "Ideas for tonight" (top; the 6 cards are a single-row **mouse-draggable carousel** inside a dashed light-brand container, + a client-side "Refreshes in Xh" countdown to next UTC midnight via `lib/time.ts` + FRESH TODAY badge) over `CatalogPreview` (recent `RecipeCard`s + "View all →" tile → /catalog). Daily rotation: `GET /api/recommendations` — lazy per-UTC-day `DailyBatch` (TheMealDB → normalize LLM → image), cached; first-of-day ~30–60s, then instant.
- **Modify with AI** (existing recipes) → `components/ModifyStudio.tsx`: **two separate cards over a backdrop** (recipe diff on the left **on white**, a Modify **dialog card** beside it — not a column inside the recipe; its own ESC/scroll-lock overlay, not `Modal`). The dialog card is **deep-sage themed** (Figma 52:283): `bg-primary` + light-sage `text-footer-muted` labels, **white** primary buttons (`text-primary-deep`) for Apply/Save, translucent-outline white for Replace, `white/NN` seg chips — its buttons are plain (not `Button`) so focus rings are `white/70` on the sage surface. The left diff card keeps `text-primary` highlights on white. Quick controls (Scale/Dietary/Simplify/Substitute) compose one NL comment applied **only on the star "Apply with AI" button press** (so the user sets several inputs first), run against the **ORIGINAL recipe** each time (re-applying reverts toggled-off controls) → Save as copy (create) / Replace original (update), both `source:'ai_modified'`. `POST /api/ai/modify` returns `{recipe, diff}`; the **diff is computed deterministically in `lib/diff.ts`** (LCS sequence align that pairs substitutions + word-level `tokenDiff`), NOT by the LLM. The import draft's lighter inline `RecipeEditForm.onModify` reads `response.recipe` and is unchanged.
- **Images:** `lib/image-provider.ts` (`IMAGE_PROVIDER` env, `gpt-image-1-mini`), public `recipe-images` bucket with unguessable keys; generated in the background after save. The picker is the shared `hooks/useImagePicker.tsx` (manual create + import draft, rendered as `RecipeEditForm`'s `imageSlot`): empty state = **three actions centered INSIDE the image container** (Upload/Generate/Skip); choosing one dismisses them → preview / a spinner + "generates after you save" / emoji, with a top-right `ImageActionMenu` to change later (`ImageMode` adds `'skip'` vs `'none'`). A chosen "Generate" sets a **client-only `imageGenerating` flag** on the cached recipe (in `useCreateRecipe`) so `RecipeCard`/`RecipeModal` show `ImageGeneratingPlaceholder` — the loader **carries over after the modal closes**, cleared when the image lands or fails. **`components/ImageActionMenu.tsx`** is the shared top-right image-overlay dropdown (generic `actions[]`, click-outside + ESC) used by both the picker and `RecipeModal` (Generate/Regenerate · Upload · Remove).
- **Recipe detail modal** `components/RecipeModal.tsx` (Figma 47:1014): two **equal** columns (`md:grid-cols-2`) inside a scroll area, over a **sticky footer**. Left (white) = serif title + inline time chip → description → **source/video link** (`SourceAttribution`, reused in place, imported only) → tag `Pill`s (click-to-filter) → **Instructions** `h3` + soft-terracotta filled-circle serif numerals. Right (**parchment** `bg-bg-page` + left border) = image w/ top-right `ImageActionMenu` → **SCALE** card (horizontal `ServingScaler`) → **ingredients table** (white card, `py-1.5` rows). The Modify/Edit/Delete **footer is a sibling after the `min-h-0 flex-1 overflow-y-auto` grid** so it stays in view while columns scroll. Don't move the ingredients table back into the left column or drop the sticky footer (both were tried in 47:1323 and reverted — DECISIONS [2026-06-01]).

### Frontend (`apps/web/src`)
- `lib/api.ts` `apiFetch` / `authedFetch` attach the JWT and throw `ApiError`. **React Query** single `['recipes']` cache + optimistic mutations (snapshot → apply → reconcile/rollback) in `hooks/useRecipes.ts`. `ToastProvider` + `AddRecipeProvider` (global chooser) at the app/layout root. **`RecipeEditForm` is THE single recipe form** (add / edit / import-review all render it); it has an opt-in `onModify` prop (wired by the import draft) that runs the current edited fields through `/api/ai/modify` and applies the result back into the form. Catalog filtering is a **dialog** (`FilterDialog`, opened by the Filter button and a "+N" overflow on the inline tag-chip row), not a dropdown. The Home recommendations carousel **auto-scrolls slowly right-to-left** (rAF advancing `scrollLeft`, seamless via a duplicated track, pauses on hover/drag, still mouse-draggable, off under `prefers-reduced-motion`) with `shimmer` loading cards. `AuthedLayout` frames a centered `max-w-[1100px]` content sheet (`bg-bg-page`) on a distinct `bg-bg-app` page so the content column reads as its own surface, and calls `usePrefetchOnLogin` to warm the `['recipes']`/`['recommendations']`/`['profile']` caches once per login (instant navigation while the cache builds). The nav's top-right `AvatarMenu` dropdown (Account / Settings / Log out — logout lives here, not the bar) routes to `/account` (personal info moved out of `About` + avatar uploader) and `/settings` (placeholder). The grouped `Footer` links to boilerplate pages `/contact`, `/faq`, `/privacy`, `/terms` (Privacy/Terms share `LegalDoc`); these are **authed routes** for now (behind login, like the rest of the app). `routes/CookMode.tsx` (`/cookmode`, the nav's 3rd link) is a **full-screen takeover** mounted **outside `AuthedLayout`** (sibling under `ProtectedRoute` → no nav/footer); currently a **non-interactive "coming soon" placeholder** (mockup wrapped `pointer-events-none`, only Exit works) — no mechanics yet. Nav + Footer inner widths are `max-w-[1148px]` so they hug the `1100` content sheet's edges; the logo links to `/home`. Favicon = `public/logo.svg`. The **avatar lives in Supabase auth `user_metadata.avatar_url`** (no DB column) — `POST /api/profile/avatar` only uploads the file (reusing the public bucket); the client persists the URL via `supabase.auth.updateUser`, so it rides in the session and the nav avatar updates reactively. New UI goes through the `figma-translation` skill (reuse `components/index.ts`, `@theme` tokens, no hex in JSX).

### Deployment → DECISIONS [2026-05-29], [stages/stage-import-progress.md](.claude/stages/stage-import-progress.md)
- **One Vercel project:** Vite SPA static (`apps/web/dist`) + the Express app as a **single serverless function** (`api/index.ts` dynamic-imports the built ESM app — CJS-can't-require-ESM shim). Hobby, `maxDuration: 60`, **Node 22** (Supabase needs a global WebSocket), Prisma `binaryTargets` incl. `rhel-openssl-3.0.x` + `vercel.json` `includeFiles`. Prod DB = dev DB → build runs `prisma generate` only. `vercel.json` rewrites `/api/(.*) → /api/index` + SPA fallback.
- **Workflow:** develop on `dev` → auto-built **protected previews**; `main` = promote-when-ready production (not pushed). Hobby has no production protection, so we stay preview-only/private until launch (then set the Supabase signup policy). Env vars must be enabled for the **Preview** environment too.

---

## Error Patterns

> Errors, causes, fixes. Added when encountered.

**Prisma `EPERM ... rename ... query_engine-windows.dll.node` on Windows**
The VSCode TypeScript Server (a `node.exe` child) loads the Prisma client DLL for IntelliSense and holds an exclusive Windows file lock. `prisma generate` writes to a `.tmp` then renames over the existing DLL — the rename fails. Reloading the VSCode window does NOT release the lock because the new TS Server respawns and re-opens the DLL immediately. Fix: find the holding PID with `Get-Process node | Where-Object Modules.FileName -like '*query_engine-windows*'` and `taskkill //PID <pid> //F` (Git Bash needs double slashes); the TS server respawns on demand and IntelliSense is unaffected. The migration itself always applies regardless — only the generated client code is blocked.

**Video import fails with "Transcript service unreachable" (esp. YouTube Shorts) — usually a TIMEOUT, not an outage**
Symptom: a video URL import 502s after ~25–28s with `Transcript service unreachable` (from `supaGet` in `lib/supadata.ts`). Supadata is almost always actually up (a keyless `GET https://api.supadata.ai/v1/transcript` returns 401 in <100ms). Cause: caption-less videos (common for Shorts) make Supadata **generate** a transcript inline instead of promptly returning 202+jobId, so the initial GET blocks past `INITIAL_TIMEOUT_MS` (25s) and the `AbortController` kills it. `supaGet` now distinguishes the abort → **504 "Transcript is taking too long — paste the caption instead"** from a real fetch failure → 502; both route the client to the manual paste/screenshot fallback. Do NOT raise the 25s timeout to "fix" it — it's tuned so transcript (≤30s budget) + extraction LLM (≤20s) stays under the 60s serverless cap (see DECISIONS, Supadata budget). **Primary mitigation:** `extractFromVideo` now reads the YouTube **description first** (`fetchYoutubeMeta` → `/youtube/video`, ~3s) and only attempts the transcript when `transcriptLanguages` is non-empty — so Shorts with the recipe in the description succeed in ~3s, and caption-less ones **fast-fail (422)** to the paste fallback instead of waiting on transcript generation.

---

## Undocumented Dependencies

> Libraries or config not in the spec but required for things to work. Added when discovered.

---

## Spec Conflicts

> Conflicts between the spec and observed reality, with the resolution. Don't silently deviate.
